# Mastodon 内容审核与用户屏蔽机制分析报告

## 概述

本报告深入分析 Mastodon 社交平台中内容审核、用户屏蔽和内容可见性设置三个层面的规则实现机制，探讨它们在多个处理环节如何共同生效、何时过滤、在哪里过滤以及规则优先级如何确定。

---

## 一、三个核心层面的规则体系

### 1. 管理员处置层面

管理员拥有最高级别的内容审核权限，可以对用户和内容进行多种处置操作：

| 操作类型 | 对内容可见性的影响 | 可逆性 |
|---------|-------------------|--------|
| **Suspend（封禁）** | 强制删除用户所有帖子（过去和未来），用户从所有时间线移除，强制所有关注者取消关注，30天内数据在后台保留 | 30天内可逆 |
| **Silence（静默）** | 用户帖子不出现在公共/联邦时间线，不出现在非关注者的通知中，但关注者仍可正常看到其帖子和互动 | 完全可逆 |
| **Freeze（冻结）** | 阻止用户进行任何账号操作（发帖、互动等），但所有内容保持不变且公开可见 | 完全可逆 |
| **Sensitive 标记** | 强制该账户的所有媒体附件始终被标记为"敏感内容"，需要用户点击展开才能查看 | 可逆 |
| **删除单个帖子** | 直接删除特定违规帖子，不可恢复 | 不可逆 |

**关键特性**：
- 即使用户屏蔽了管理员/版主，管理员在**审核界面**仍能看到该用户的**公开帖子**和**被举报的帖子**
- 这是一个设计上的权衡：既保护普通用户的屏蔽功能，又确保管理员能够履行内容审核职责

### 2. 用户互相屏蔽层面

用户可以自主控制看到哪些内容，主要有两种方式：

#### Block（屏蔽）

屏蔽是最强的用户级过滤机制，效果包括：
- 被屏蔽用户不会出现在你的**主页时间线**
- 你不会看到其他人**转发（Boost）**被屏蔽用户的内容
- 你不会看到其他人**提及**被屏蔽用户
- 被屏蔽用户不会出现在**公共/联邦时间线**
- 你不会收到被屏蔽用户的**通知**
- 被屏蔽用户无法**提及或关注**你

#### Mute（静音）

静音是相对温和的过滤方式：
- 被静音用户的帖子不会出现在你的时间线
- 但你仍可以**选择性地查看**他们的内容
- 与屏蔽不同，静音不会阻止对方看到你的内容

#### 域级屏蔽（Domain Block/Mute）

用户可以对整个实例域名进行屏蔽或静音：
- 阻止查看该域名下所有用户的帖子
- 阻止该域名用户关注你
- **注意**：不阻止该域名用户查看你的公开资料
- **注意**：域级屏蔽不会像用户级屏蔽那样通知对方服务器，因此对方可能仍能看到你的内容

### 3. 内容可见性设置层面

Mastodon 为每个帖子（Toot）提供四种精细的可见性控制：

| 可见性级别 | 图标 | 可见范围 | 时间线出现 | 联邦传播 |
|-----------|------|---------|-----------|---------|
| **Public（公开）** | 🌐 | 所有人 | 主页、公共、联邦时间线 | 完全联邦 |
| **Unlisted（不公开）** | 🔓 | 所有人（通过链接/个人主页） | 仅主页时间线 | 联邦但不进入公共时间线 |
| **Private/Followers-only（私密）** | 🔒 | 仅关注者 + 被提及用户 | 仅关注者的主页时间线 | 不联邦 |
| **Direct（私信）** | ✉️ | 仅被提及用户 | 不进入任何时间线 | 不联邦 |

**补充说明**：
- **Public**：最大可见度，出现在所有公共时间线，完全联邦到其他实例
- **Unlisted**：适合回复和对话，保持时间线整洁，仍可通过个人主页或链接访问
- **Private**：适合仅限粉丝的内容更新，非关注者即使有链接也无法查看
- **Direct**：适合私密对话，类似私信功能

---

## 二、过滤时机与位置

### 1. 数据库查询层面（最底层）

Mastodon 在 SQL 查询层面应用过滤条件：

```sql
-- 公共时间线查询的核心逻辑
SELECT * FROM statuses 
WHERE visibility = 'public' 
  AND (-- 过滤被屏蔽用户
       account_id NOT IN (SELECT target_account_id FROM blocks WHERE account_id = :current_user_id)
       -- 过滤被静音用户
       AND account_id NOT IN (SELECT target_account_id FROM mutes WHERE account_id = :current_user_id)
       -- 过滤被屏蔽域名
       AND domain NOT IN (SELECT domain FROM domain_blocks))
  AND created_at <= :timestamp
ORDER BY created_at DESC
LIMIT 20;
```

**过滤位置**：`app/models/status.rb` 中的各类 scope 定义
- `public_scope` - 筛选公开帖子
- `unlisted_scope` - 筛选不公开帖子
- `private_scope` - 筛选私密帖子
- `direct_scope` - 筛选私信帖子

### 2. 时间线构建层面

**Feed Service** 在构建不同时间线时应用特定过滤逻辑：

#### Home Timeline（主页时间线）
构建逻辑位置：`app/services/feed_service.rb`

过滤条件包括：
1. 只包含关注用户的帖子
2. 排除被屏蔽用户的帖子
3. 排除被静音用户的帖子
4. 排除被屏蔽域名的帖子
5. 应用用户自定义过滤器

#### Public Timeline（公共时间线）
过滤条件包括：
1. 只包含 `visibility = 'public'` 的帖子
2. 排除被屏蔽用户的帖子
3. 排除被屏蔽域名的帖子
4. 可以选择 `local=true` 只显示本地实例帖子

#### Federated Timeline（联邦时间线）
- 类似公共时间线，但包含所有已知实例的公开帖子
- 管理员可以通过 `Limited` 模式限制某些用户的帖子不出现在联邦时间线

### 3. 自定义过滤器层面

用户可以创建基于关键词的自定义过滤器，位置：`app/models/custom_filter.rb`

**过滤器属性**：
- `context`：应用场景数组，可选值：
  - `home` - 主页时间线
  - `notifications` - 通知
  - `public` - 公共时间线
  - `thread` - 帖子详情页/对话
- `action`：过滤动作
  - `warn` - 显示警告提示（默认）
  - `hide` - 完全隐藏

**V2 过滤器特性**：
- 由服务器端处理
- 为匹配的状态添加 `filtered` 属性
- 客户端根据该属性决定如何展示

### 4. 通知过滤层面

通知系统在生成通知时也会应用过滤：
- 排除被屏蔽用户产生的通知
- 排除被静音用户产生的通知
- 可以单独设置是否接收特定类型通知（关注、转发、收藏、提及等）

---

## 三、规则优先级体系

### 优先级金字塔（从高到低）

```
                    ┌─────────────────────┐
                    │   1. 管理员封禁     │  ← 最高优先级
                    │   (Suspend)         │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │   2. 管理员静默     │
                    │   (Silence)         │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │   3. 内容删除       │
                    │   (Delete Status)   │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │   4. Sensitive 标记 │
                    │   (媒体强制敏感)     │
                    └──────────┬──────────┘
                               │
            ┌──────────────────┼──────────────────┐
            │                  │                  │
   ┌────────▼────────┐ ┌──────▼──────┐ ┌────────▼────────┐
   │ 5. 用户级屏蔽   │ │5. 域级屏蔽  │ │5. 域名静默       │
   │ (Block)         │ │(Domain Block)│ │(Domain Mute)    │
   └────────┬────────┘ └──────┬──────┘ └────────┬────────┘
            │                  │                  │
            └──────────────────┼──────────────────┘
                               │
                    ┌──────────▼──────────┐
                    │   6. 用户级静音      │
                    │   (Mute)             │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │   7. 内容可见性设置   │
                    │   (Visibility)       │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │   8. 自定义过滤器    │
                    │   (Custom Filter)    │
                    └─────────────────────┘  ← 最低优先级
```

### 详细优先级说明

| 优先级 | 规则类型 | 生效机制 | 可否覆盖 |
|-------|---------|---------|---------|
| **1** | 管理员封禁 (Suspend) | 数据层面删除/隔离，账号从系统中"消失" | 不可覆盖 |
| **2** | 管理员静默 (Silence) | 时间线查询时排除，限制公共可见性 | 关注者仍可见 |
| **3** | 内容删除 | 数据物理删除（或软删除标记） | 不可恢复 |
| **4** | Sensitive 标记 | 媒体附件强制敏感标记，需点击展开 | 用户可选择展开 |
| **5** | 用户屏蔽 (Block) | 查询时排除对方账号ID | 管理员审核界面例外 |
| **5** | 域级屏蔽 (Domain Block) | 查询时排除整个域名 | 部分场景可能穿透 |
| **6** | 用户静音 (Mute) | 时间线过滤，但可主动查看 | 用户可绕过 |
| **7** | 内容可见性 (Visibility) | 基于 `visibility` 字段的基础过滤 | 公开设置无限制 |
| **8** | 自定义过滤器 | 关键词匹配，可选隐藏/警告 | 用户可选择查看 |

### 特殊情况与例外

#### 情况1：用户屏蔽了管理员
- **普通界面**：管理员无法看到该用户的帖子（遵循普通屏蔽规则）
- **审核界面**：管理员可以看到该用户的**公开帖子**和**被举报帖子**
- **设计意图**：确保内容审核能力不被用户规避

#### 情况2：域级屏蔽的局限性
域级屏蔽存在一些"漏洞"：
- 被屏蔽域名的用户**未登录时**仍能看到你的公开帖子
- 被屏蔽域名的管理员可以通过其用户的**关注关系**看到你的帖子
- 被屏蔽用户可以创建**新账号**继续查看

#### 情况3：跨实例交互
在联邦网络中，过滤规则的传播存在差异：
- **用户级屏蔽**：会通知对方实例（部分实例可能尊重，部分可能不）
- **域级屏蔽**：不会通知对方实例，完全是本地行为
- **内容可见性**：`public`/`unlisted` 会联邦，`private`/`direct` 不会

---

## 四、多环节协同过滤流程

### 典型场景：用户查看主页时间线

```
┌─────────────────────────────────────────────────────────────────┐
│                    主页时间线加载流程                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第一层：数据库查询过滤（app/models/status.rb）                   │
│  ─────────────────────────────────────────────────────────────  │
│  • WHERE visibility IN ('public', 'unlisted', 'private')        │
│  • AND account_id IN (关注用户列表)                               │
│  • AND account_id NOT IN (被屏蔽用户列表)                         │
│  • AND domain NOT IN (被屏蔽域名列表)                             │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第二层：用户静音过滤（app/models/mute.rb）                       │
│  ─────────────────────────────────────────────────────────────  │
│  • 从结果中排除被静音用户的帖子                                    │
│  • 注意：静音用户的转发仍可能出现（如果转发者未被静音）            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第三层：私密内容权限检查                                          │
│  ─────────────────────────────────────────────────────────────  │
│  • 对于 visibility = 'private' 的帖子                            │
│  • 检查：当前用户是否是作者的关注者 OR 当前用户被提及              │
│  • 检查：作者账号是否被冻结/封禁                                   │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第四层：自定义过滤器（app/models/custom_filter.rb）              │
│  ─────────────────────────────────────────────────────────────  │
│  • 匹配用户定义的关键词/短语/话题                                  │
│  • 检查过滤器 context 是否包含 'home'                              │
│  • action = 'hide' → 从结果中移除                                 │
│  • action = 'warn' → 保留但标记为 filtered                        │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第五层：敏感内容处理（前端展示层）                                │
│  ─────────────────────────────────────────────────────────────  │
│  • sensitive = true OR 有 spoiler_text                           │
│  • 前端显示内容警告（CW）遮罩                                      │
│  • 用户点击"显示"才会展开内容                                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │   最终展示给用户   │
                    └──────────────────┘
```

### 典型场景：用户查看公共时间线

```
┌─────────────────────────────────────────────────────────────────┐
│                    公共时间线加载流程                              │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第一层：数据库查询过滤                                            │
│  ─────────────────────────────────────────────────────────────  │
│  • WHERE visibility = 'public'                                   │
│  • AND account_id NOT IN (被屏蔽用户列表)                         │
│  • AND domain NOT IN (被屏蔽域名列表)                             │
│  • AND (NOT silenced OR 是本地实例 OR 当前用户关注该用户)         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第二层：管理员静默处理                                            │
│  ─────────────────────────────────────────────────────────────  │
│  • 被静默用户的帖子不会出现在公共时间线                            │
│  • 例外：如果当前用户关注了该静默用户，则可以看到                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│  第三层：自定义过滤器（context = 'public'）                       │
│  ─────────────────────────────────────────────────────────────  │
│  • 应用针对公共时间线的过滤器                                      │
│  • 不包含针对 'home' 或 'notifications' 的过滤器                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │   最终展示给用户   │
                    └──────────────────┘
```

---

## 五、关键技术实现点

### 1. 模型关联设计

```ruby
# app/models/account.rb
class Account < ApplicationRecord
  # 屏蔽关系
  has_many :block_relationships, class_name: 'Block', foreign_key: :account_id
  has_many :blocked_accounts, through: :block_relationships, source: :target_account
  
  has_many :blocked_by_relationships, class_name: 'Block', foreign_key: :target_account_id
  has_many :blocked_by_accounts, through: :blocked_by_relationships, source: :account
  
  # 静音关系
  has_many :mute_relationships, class_name: 'Mute', foreign_key: :account_id
  has_many :muted_accounts, through: :mute_relationships, source: :target_account
end

# app/models/block.rb
class Block < ApplicationRecord
  belongs_to :account
  belongs_to :target_account, class_name: 'Account'
end

# app/models/domain_block.rb
class DomainBlock < ApplicationRecord
  # 三种 severity:
  # - silence: 静默该域名用户
  # - suspend: 封禁该域名所有账号
  # - noop: 仅记录，不执行实际操作
  enum severity: { silence: 0, suspend: 1, noop: 2 }
end
```

### 2. 查询 Scope 设计

```ruby
# app/models/status.rb
class Status < ApplicationRecord
  scope :public_scope, -> { where(visibility: :public) }
  scope :unlisted_scope, -> { where(visibility: :unlisted) }
  scope :private_scope, -> { where(visibility: :private) }
  scope :direct_scope, -> { where(visibility: :direct) }
  
  scope :without_silenced, -> {
    # 排除被静默的账号，但保留本地账号和被关注的账号
    left_joins(:account).where(
      'accounts.silenced_at IS NULL OR accounts.id IN (?) OR accounts.domain IS NULL',
      # 当前用户的关注列表
    )
  }
  
  scope :without_blocked_by, ->(account) {
    where.not(account_id: account.blocking.pluck(:target_account_id))
  }
  
  scope :without_domain_blocked, ->(account) {
    left_joins(:account).where.not(
      'accounts.domain IN (?)',
      account.domain_blocks.pluck(:domain)
    )
  }
end
```

### 3. 管理员审核特殊处理

```ruby
# 在审核界面中，跳过普通用户的屏蔽规则
# app/controllers/admin/reports_controller.rb 类似逻辑

def moderation_view_statuses
  # 管理员查看被举报用户的帖子
  # 不应用普通的屏蔽/静音过滤
  @statuses = @account.statuses
    .where(visibility: [:public, :unlisted])  # 仍尊重内容可见性
    .order(created_at: :desc)
    .limit(20)
end
```

---

## 六、总结与设计洞察

### 核心设计原则

1. **分层防御**
   - 从数据库查询到前端展示，多层过滤确保安全性
   - 每层有明确的职责和边界

2. **优先级明确**
   - 管理员操作 > 用户控制 > 内容设置 > 自定义过滤
   - 确保治理能力不被用户行为规避

3. **透明度与选择权**
   - 用户可以看到哪些内容被过滤（自定义过滤器的 `warn` 模式）
   - 用户可以选择展开敏感内容

4. **联邦特性考虑**
   - 域级屏蔽是本地行为，不依赖对方实例配合
   - 用户级屏蔽尝试通知对方，但不强求

### 潜在的局限与权衡

| 场景 | 当前行为 | 权衡考虑 |
|-----|---------|---------|
| 用户屏蔽管理员 | 普通界面不可见，审核界面可见 | 保护用户隐私 vs 确保审核能力 |
| 域级屏蔽的"漏洞" | 对方管理员仍可能看到 | 完全隔离不可行 vs 保护大多数场景 |
| 公开帖子的可访问性 | 未登录/其他实例仍可见 | 符合"公开"的定义 vs 用户期望的"隐私" |
| 静音用户的转发 | 可能通过第三方看到 | 完全屏蔽的复杂性 vs 用户体验 |

### 最佳实践建议

对于普通用户：
1. **使用屏蔽**而非静音来应对严重骚扰
2. 对**整个问题实例**使用域级屏蔽
3. 合理设置帖子**可见性**（尤其是敏感内容）
4. 使用**自定义过滤器**处理特定关键词/话题

对于管理员：
1. **Silence** 适合轻度违规，给用户改正机会
2. **Suspend** 仅用于严重违规，注意30天窗口期
3. 定期检查**审核界面**，包括被用户屏蔽的内容
4. 考虑实例级的**域屏蔽策略**而非仅依赖用户自主屏蔽

---

## 附录：相关核心文件位置

| 功能模块 | 文件路径 | 主要职责 |
|---------|---------|---------|
| 状态模型 | `app/models/status.rb` | 可见性字段、查询 Scope 定义 |
| 账户模型 | `app/models/account.rb` | 屏蔽/静音关系定义 |
| 屏蔽模型 | `app/models/block.rb` | 用户屏蔽关系存储 |
| 静音模型 | `app/models/mute.rb` | 用户静音关系存储 |
| 域屏蔽模型 | `app/models/domain_block.rb` | 域名级屏蔽规则 |
| 自定义过滤器 | `app/models/custom_filter.rb` | 用户关键词过滤 |
| Feed服务 | `app/services/feed_service.rb` | 时间线构建逻辑 |
| 管理员控制器 | `app/controllers/admin/` | 管理后台审核操作 |
| 时间线API | `app/controllers/api/v1/timelines/` | 公共/主页时间线接口 |

---

*报告生成日期：2026-05-02*
*基于 Mastodon 官方文档和代码架构分析*
